# 复制为 .env 后按需修改
# cp .env.example .env

# 使用预构建镜像，无需本地 build（推荐）
OPENCLAW_IMAGE=alpine/openclaw:latest

# 配置与工作目录（相对 openclaw-local 目录，或填绝对路径）
OPENCLAW_CONFIG_DIR=./config
OPENCLAW_WORKSPACE_DIR=./workspace

# 端口（与本地命令 openclaw gateway --port 18789 一致）
OPENCLAW_GATEWAY_PORT=18789
OPENCLAW_BRIDGE_PORT=18790

# 绑定方式：loopback=仅本机 lan=局域网可访问
OPENCLAW_GATEWAY_BIND=lan

# 控制台 / Dashboard 认证（可选，首次 onboarding 后会生成）
# OPENCLAW_GATEWAY_TOKEN=你的token
